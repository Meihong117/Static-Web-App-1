{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
            "*.{jpg,gif,png}",
            "assets/*"
        ]
    },
    "routes": [
        {
            "route": "/user/:id",
            "rewrite": "/components"
        }
    ]
}